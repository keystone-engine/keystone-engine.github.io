This directory contains sample code for Keystone engine.

- On Linux/Mac OSX/*nix, compile with "make" as following:

    $ make

    Then run the resulted "test1" & "test2"

- On Windows, compile with "nmake.bat" as following (CMake is required):

    $ mkdir build
    $ cd build
    $ ../nmake.bat

    Then run the resulted "test1.exe" & "test2.exe"
